/**
 * Interface representing the structure of a user object, which includes properties such as id, fullName, email, and money.
 */
export interface UserInterface {
  id: string;
  fullName: string;
  email: string;
  money: number;
}
