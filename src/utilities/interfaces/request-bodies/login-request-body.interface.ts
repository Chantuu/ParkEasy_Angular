/**
 * Interface representing the structure of a login request body, which includes properties for email and password.
 */
export interface LoginRequestBodyInterface {
  email: string;
  password: string;
}
