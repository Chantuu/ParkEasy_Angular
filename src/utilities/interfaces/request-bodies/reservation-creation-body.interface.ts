/**
 * Interface representing the structure of a reservation creation request body, which includes a single property parkingSpotId.
 */
export interface ReservationCreationBodyInterface {
  parkingSpotId: string;
}
