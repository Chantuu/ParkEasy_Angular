/**
 * Interface representing the structure of a user logout response, which includes a status property of type string
 * and a message property of type string that provides additional information about the logout response.
 */
export interface UserLogoutResponse {
  status: string;
  message: string;
}
